# bootstrap.yml - Loads before application.yml to set up config server connection
spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: configuser
      password: configpass
      fail-fast: true